# CSS Quick Reference Card

## ⚡ Quick Commands

```bash
npm run watch      # Start auto-rebuild (keep running)
npm run build:css  # Rebuild CSS manually
npm run check:css  # Check if rebuild needed
npm run rebuild    # Rebuild with confirmation
```

## 📝 Workflow

1. **Edit**: `frontend/index.css` (source file)
2. **Rebuild**: `npm run build:css` (or use watch mode)
3. **Refresh**: Hard refresh browser (Cmd+Shift+R)

## ⚠️ Warning Signs

- **Styles not updating** → Run `npm run build:css`
- **Admin notice** → CSS needs rebuilding
- **Weird spacing/layout** → Check if CSS is built

## 🎯 Remember

- ✅ Always edit `index.css`
- ❌ Never edit `build.css` (it's auto-generated)
- 🔄 Always rebuild after editing CSS
- 👀 Admin will warn you if CSS is out of sync

---
**Full guide**: See [CSS-WORKFLOW.md](./CSS-WORKFLOW.md)

