{
  "name": "thriving-studio-frontend",
  "version": "1.0.0",
  "description": "Legacy frontend package file. Use root package.json scripts as canonical build commands.",
  "main": "index.js",
  "scripts": {
    "dev": "npm --prefix .. run dev",
    "build": "npm --prefix .. run build:css",
    "watch": "npm --prefix .. run watch:css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["wordpress", "theme", "frontend", "tailwind", "vite"],
  "author": "Thriving Studio Team",
  "license": "GPL-2.0-or-later",
  "type": "commonjs",
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.3",
    "vite": "^6.3.5"
  }
}
